import Classes from '@features/classes';
import BaseLayout from '@layouts/base';

const ClassesPage = () => {
  return (
    <BaseLayout>
      <Classes />
    </BaseLayout>
  );
};

export default ClassesPage;
